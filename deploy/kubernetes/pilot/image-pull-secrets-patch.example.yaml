# Optional patch for a private registry. Copy this file to an environment
# overlay and add it under `patches` only after creating the Secret out of
# band. The Secret value must not be committed.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: discussion-map-pilot
spec:
  template:
    spec:
      imagePullSecrets:
        - name: discussion-map-registry
