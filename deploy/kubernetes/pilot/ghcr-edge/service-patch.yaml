apiVersion: v1
kind: Service
metadata:
  name: ronro-pilot
spec:
  type: NodePort
  ports:
    - name: http
      port: 8000
      targetPort: http
    - name: websocket
      port: 8765
      targetPort: websocket
