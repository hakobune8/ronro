apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ronro-pilot

resources:
  - ../../base

images:
  - name: registry.example.invalid/ronro
    newName: ghcr.io/hakobune8/ronro
    digest: sha256:8e22329f4e975f994d36ea68bf5f822028020bc781add4988e618fe244c32c71

patches:
  - path: deployment-patch.yaml
  - path: service-patch.yaml
  - path: ingress-patch.yaml
  # Keep the edge-cluster EndpointSlice's fixed NodePort contract.
  - target:
      version: v1
      kind: Service
      name: ronro-pilot
    patch: |-
      - op: add
        path: /spec/ports/0/nodePort
        value: 30100
      - op: add
        path: /spec/ports/1/nodePort
        value: 30101
