# Template only. Do not apply this file with a real value and do not commit
# a populated Secret. Create the Secret out-of-band as described in the
# deployment guide.
apiVersion: v1
kind: Secret
metadata:
  name: ronro-openai
  namespace: ronro-pilot
type: Opaque
stringData:
  OPENAI_API_KEY: ""
