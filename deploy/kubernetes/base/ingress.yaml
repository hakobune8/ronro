apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ronro-pilot
  labels:
    app.kubernetes.io/name: ronro
  annotations:
    # These annotations are for ingress-nginx. Set ingressClassName and
    # annotations in the pilot overlay to match the installed controller.
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "30"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "1200"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "1200"
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ronro-pilot.example.invalid
      secretName: ronro-pilot-tls
  rules:
    - host: ronro-pilot.example.invalid
      http:
        paths:
          - path: /live
            pathType: Prefix
            backend:
              service:
                name: ronro-pilot
                port:
                  name: websocket
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ronro-pilot
                port:
                  name: http
