apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: discussion-map-pilot-evaluation
  labels:
    app.kubernetes.io/name: discussion-map-ai-facilitator
    app.kubernetes.io/component: evaluation-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
